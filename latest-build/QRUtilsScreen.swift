import SwiftUI
struct QRUtilsScreen: View {
    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 12) {
            }.padding()
        }
        .navigationTitle("QRUtils")
    }
}