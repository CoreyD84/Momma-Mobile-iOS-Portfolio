import SwiftUI

struct QRUtilsScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("QRUtils").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("QRUtils")
    }
}