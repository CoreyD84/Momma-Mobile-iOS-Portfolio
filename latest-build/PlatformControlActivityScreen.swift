import SwiftUI

struct PlatformControlActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("PlatformControlActivity")
    }
}