import SwiftUI

struct OnlineSafetyFragmentScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("OnlineSafetyFragment")
    }
}