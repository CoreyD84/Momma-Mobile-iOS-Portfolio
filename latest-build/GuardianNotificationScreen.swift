import SwiftUI

struct GuardianNotificationScreen: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("GuardianNotification")
            Text("Service is not available on iOS.")
        }.padding().navigationTitle("GuardianNotification")
    }
}
