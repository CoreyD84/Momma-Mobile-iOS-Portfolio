import SwiftUI

struct SmsDetectionsActivityScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("SmsDetectionsActivity").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("SmsDetectionsActivity")
    }
}