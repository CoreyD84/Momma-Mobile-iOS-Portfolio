import SwiftUI
struct SmsDetectionsActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("SmsDetectionsActivity")
    }
}