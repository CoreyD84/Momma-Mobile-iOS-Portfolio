import SwiftUI
struct ExampleInstrumentedTestScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("ExampleInstrumentedTest")
    }
}