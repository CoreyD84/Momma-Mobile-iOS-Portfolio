import SwiftUI
struct LinkedChildrenFragmentScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("LinkedChildrenFragment")
    }
}