import SwiftUI

struct OnlineSafetyActivityScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("OnlineSafetyActivity").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("OnlineSafetyActivity")
    }
}