import SwiftUI
struct OnlineSafetyActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("OnlineSafetyActivity")
    }
}