import SwiftUI

struct MainActivityScreen: View {

    var body: some View {
        VStack(spacing: 20) {
            Text("MainActivity").font(.largeTitle).bold().padding(.bottom, 10)
            Spacer()
        }.padding()
    }
}