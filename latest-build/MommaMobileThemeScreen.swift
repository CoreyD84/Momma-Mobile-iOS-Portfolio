import SwiftUI

struct MommaMobileThemeScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("MommaMobileTheme")
    }
}