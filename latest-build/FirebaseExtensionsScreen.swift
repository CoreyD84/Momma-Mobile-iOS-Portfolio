import SwiftUI

struct FirebaseExtensionsScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("FirebaseExtensions").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("FirebaseExtensions")
    }
}