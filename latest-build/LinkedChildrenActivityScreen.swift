import SwiftUI
struct LinkedChildrenActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("LinkedChildrenActivity")
    }
}