import SwiftUI
struct SubscriptionExpiredActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("SubscriptionExpiredActivity")
    }
}