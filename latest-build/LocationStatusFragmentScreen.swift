import SwiftUI
struct LocationStatusFragmentScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("LocationStatusFragment")
    }
}