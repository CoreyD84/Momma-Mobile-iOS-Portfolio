import SwiftUI
struct CloudinaryConfigScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Button(action: {}) { Text("dus3l44db").padding().frame(maxWidth: .infinity).background(Color.blue.opacity(0.1)).cornerRadius(10) }
            Button(action: {}) { Text("758654299117598").padding().frame(maxWidth: .infinity).background(Color.blue.opacity(0.1)).cornerRadius(10) }
            Button(action: {}) { Text("soullink_photos").padding().frame(maxWidth: .infinity).background(Color.blue.opacity(0.1)).cornerRadius(10) }
            Spacer()
        }.padding().navigationTitle("CloudinaryConfig")
    }
}