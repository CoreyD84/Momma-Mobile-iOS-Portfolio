import SwiftUI

struct UserScreen: View {

    var body: some View {
        VStack(spacing: 20) {
            Text("User").font(.largeTitle).bold().padding(.bottom, 10)
            Spacer()
        }.padding()
    }
}