import SwiftUI

struct AppModuleScreen: View {

    var body: some View {
        VStack(spacing: 20) {
            Text("AppModule").font(.largeTitle).bold().padding(.bottom, 10)
            Spacer()
        }.padding()
    }
}