import SwiftUI

struct LocationTrackerScreen: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("LocationTracker")
            Text("Service is not available on iOS.")
        }.padding().navigationTitle("LocationTracker")
    }
}
