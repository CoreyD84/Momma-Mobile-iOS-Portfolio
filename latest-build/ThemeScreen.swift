import SwiftUI

struct ThemeScreen: View {

    var body: some View {
        VStack(spacing: 20) {
            Text("Theme").font(.largeTitle).bold().padding(.bottom, 10)
            Spacer()
        }.padding()
    }
}