import SwiftUI
struct ConsentOverviewFragmentScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("ConsentOverviewFragment")
    }
}