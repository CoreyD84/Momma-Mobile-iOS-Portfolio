import SwiftUI

struct HeartbeatMonitorScreen: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("HeartbeatMonitor")
            Text("Service is not available on iOS.")
        }.padding().navigationTitle("HeartbeatMonitor")
    }
}
