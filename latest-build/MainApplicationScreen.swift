import SwiftUI

struct MainApplicationScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("MainApplication")
    }
}