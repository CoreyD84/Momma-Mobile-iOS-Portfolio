import SwiftUI

struct ComposePlatformControlFragmentScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("ComposePlatformControlFragment").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("ComposePlatformControlFragment")
    }
}