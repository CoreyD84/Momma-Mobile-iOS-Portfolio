import SwiftUI

struct FlaggedMessagesActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("FlaggedMessagesActivity")
    }
}