import SwiftUI
struct SmsDetectionsFragmentScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("SmsDetectionsFragment")
    }
}