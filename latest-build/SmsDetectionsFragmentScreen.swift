import SwiftUI

struct SmsDetectionsFragmentScreen: View {

    var body: some View {
        ScrollView {
            VStack(alignment: .leading, spacing: 15) {
                Text("SmsDetectionsFragment").font(.title2).bold().padding(.bottom)
            }.padding()
        }
        .navigationTitle("SmsDetectionsFragment")
    }
}