import SwiftUI

struct ExampleUnitTestScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding()
        .navigationTitle("ExampleUnitTest")
    }
}