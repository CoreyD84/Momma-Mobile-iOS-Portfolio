import SwiftUI
struct FreezeReflexScreen: View { var body: some View { VStack(spacing: 15) {
            Text("FreezeReflex").font(.headline)
            Spacer()
        }.padding().navigationTitle("FreezeReflex") } }