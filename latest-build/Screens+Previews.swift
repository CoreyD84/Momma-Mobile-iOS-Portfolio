import SwiftUI

struct SafetyDashboardScreen_Previews: PreviewProvider {
    static var previews: some View {
        // Targets the Screen view directly using its internal @StateObject
        SafetyDashboardScreen()
            .previewDisplayName("SafetyDashboard")
    }
}