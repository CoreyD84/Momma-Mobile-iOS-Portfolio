import SwiftUI
struct MessageScannerActivityScreen: View { var body: some View { VStack(spacing: 15) {
            Text("MessageScannerActivity").font(.headline)
            Spacer()
        }.padding().navigationTitle("MessageScannerActivity") } }