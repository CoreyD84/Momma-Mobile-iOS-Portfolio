import SwiftUI
struct MessageScannerActivityScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("MessageScannerActivity")
    }
}