import SwiftUI

struct SafeScopeVpnScreen: View {
    var body: some View {
        VStack(spacing: 12) {
            Text("SafeScopeVpn")
            Text("Service is not available on iOS.")
        }.padding().navigationTitle("SafeScopeVpn")
    }
}
