import SwiftUI
struct GhostDetectionWorkerScreen: View {
    var body: some View {
        VStack(spacing: 15) {
            Spacer()
        }.padding().navigationTitle("GhostDetectionWorker")
    }
}